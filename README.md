# 🗑️ Smart Trash Bin (ถังขยะอัจฉริยะ 4 ประเภท)

โปรเจกต์ถังขยะอัจฉริยะ (Smart Trash Bin) ที่สามารถเปิด-ปิดฝาอัตโนมัติเมื่อมีคนนำขยะมาทิ้ง หรือควบคุมการเปิด-ปิดแบบแมนนวลผ่าน Web Dashboard พร้อมระบบเก็บประวัติการใช้งานและข้อมูลสถานะของถังขยะแบบเรียลไทม์

ระบบรองรับถังขยะ 4 ประเภทหลัก: 
1. 🟡 ถังทั่วไป
2. 🟢 ถังรีไซเคิล
3. 🔴 ถังอันตราย
4. 🟤 ถังอินทรีย์

---

## 🛠️ Tech Stack (เครื่องมือและเทคโนโลยีที่ใช้)

### Hardware (ฮาร์ดแวร์)
- **Microcontroller**: Raspberry Pi Pico W (ใช้ Arduino Core)
- **Sensors**: Ultrasonic Sensor (HC-SR04) x 4 (สำหรับตรวจจับระยะห่าง)
- **Actuators**: Servo Motor x 4 (สำหรับเปิดและปิดฝาถัง)

### Software & Services (ซอฟต์แวร์และบริการ)
- **Backend / API**: Vercel Serverless Functions (Node.js)
- **Database**: Supabase (PostgreSQL) สำหรับการจัดการฐานข้อมูลและบันทึกประวัติ (Logs)
- **Frontend / Dashboard**: HTML, Vanilla JS เสิร์ฟเป็น Static site รูปแบบโฟลเดอร์ Public
- **Communication Protocol**: HTTP/HTTPS REST API (ส่งข้อมูลรูปแบบ JSON)

---

## ⚙️ Workflow การทำงานของระบบ

1. **การตรวจจับระยะ (Sensing)**:
   - บอร์ด Pico W จะใช้เซ็นเซอร์ Ultrasonic ทั้ง 4 ตัววัดระยะห่าง (Distance) ตลอดเวลาในลูป (ทุกๆ 50ms)
2. **การทำงานของถังขยะ (Bin Operation)**:
   - **โหมด Auto**: ถ้าระยะที่วัดได้น้อยกว่าระยะเกณฑ์ (Threshold ค่าเริ่มต้นคือ 30 cm) บอร์ดจะสั่งให้ Servo เปิดฝาถังค้างไว้เป็นเวลา 3 วินาที จากนั้นจะสวิงปิดฝาอัติโนมัติ
   - **โหมด Manual**: การเปิด-ปิดจะรอรับคำสั่ง `manual_open` ผ่านหน้า Web Dashboard ของผู้ใช้เท่านั้น 
3. **การส่งข้อมูล (Data Sync)**:
   - ทุกๆ 500ms บอร์ด Pico W จะประสานข้อมูลระยะห่าง (`distance_cm`) และสถานะฝาถัง (`lid_open`) ไปยัง API ของ Vercel (`POST /api/device`)
   - Vercel API จะอัปเดตข้อมูลบน **Supabase** (ตาราง `bins` และ `bin_logs`) และตอบกลับคำสั่งที่ถูกตั้งค่าใหม่ลงมาให้ฮาร์ดแวร์ เช่น โหมด (auto/manual), ค่า Threshold ใหม่ และคำสั่งเปิดฝาแบบ Manual
4. **Web Dashboard**:
   - ผู้ใช้งานเข้าฝั่ง Frontend จะดึงข้อมูลสถานะล่าสุดผ่าน API `GET /api/device` 
   - สามารถส่งคำสั่งผ่าน API `POST /api/device` (พร้อม JWT Token) เช่น ปรับระยะเกณฑ์ Threshold, สลับโหมด, หรือสั่งเปิด/ปิดฝาถังด้วยปุ่มบนซอฟต์แวร์

---

## ⚡ การต่ออุปกรณ์ (Wiring & Pin Map)

ใช้บอร์ด **Raspberry Pi Pico W** ในการควบคุมอุปกรณ์ทั้งหมด ซึ่งแบ่งพินเชื่อมต่อออกดังนี้:

| ถังขยะ (Bin) | ชื่อในฐานข้อมูล | พิน Ultrasonic TRIG | พิน Ultrasonic ECHO | พิน Servo (PWM) |
|-------------|--------------|--------------------|--------------------|----------------|
| **Bin 1**   | ถังทั่วไป       | `GPIO 2`           | `GPIO 3`           | `GPIO 4`       |
| **Bin 2**   | ถังรีไซเคิล      | `GPIO 6`           | `GPIO 7`           | `GPIO 8`       |
| **Bin 3**   | ถังอันตราย      | `GPIO 10`          | `GPIO 11`          | `GPIO 12`      |
| **Bin 4**   | ถังอินทรีย์      | `GPIO 14`          | `GPIO 15`          | `GPIO 16`      |

*องศาการทำงานของ Servo*: 
- ปิดฝา (Close): `0` องศา
- เปิดฝา (Open): `180` องศา

---

## 🚀 การติดตั้งและตั้งค่าโปรเจกต์ (Setup Guide)

### 1. การตั้งค่า ฐานข้อมูล (Supabase)
1. ไปที่เว็บไซต์ Supabase และสร้าง Project ใหม่
2. นำโค้ดจากไฟล์ `supabase_schema.sql` ไปรันในแท็บ **SQL Editor** เพื่อสร้างตาราง `bins` และ `bin_logs` พร้อมทั้งใส่ข้อมูลตั้งต้นของถังทั้งหมด 4 ใบ
3. (ตัวเลือกเสริม) อนุญาตแชร์ข้อมูลโดยเปิดใช้งาน Realtime สำหรับตาราง `bins`

### 2. การตั้งค่า API (Vercel)
1. คัดลอกไฟล์ `.env.example` มาสร้างใหม่เป็นไฟล์ `.env` ในเครื่องหรือ Dashboard
2. กรอกค่าตัวแปรดังนี้:
   - `NEXT_PUBLIC_SUPABASE_URL`: ลิงก์ URL ของโปรเจกต์ Supabase
   - `SUPABASE_SERVICE_ROLE_KEY`: กุญแจ Service Role Key ลับระดับสูงจาก Supabase
   - `DEVICE_SECRET`: รหัสลับที่ใช้รับส่งข้อมูลกับบอร์ดฮาร์ดแวร์ (เพื่อความมั่นคงปลอดภัย)
   - `JWT_SECRET`: รหัสลับที่ใช้เพื่อรับรองการเข้าสู่ระบบ Web Dashboard
3. ติดตั้งและ Deploy รูปแบบโปรเจกต์ขึ้นไปยัง Vercel 

### 3. การอัปโหลดโค้ดลงคอมพิวเตอร์บอร์ดฮาร์ดแวร์ (Raspberry Pi Pico W)
1. เปิดไฟล์ `public/smart_trash_bin.ino` ด้วยโปรแกรม Arduino IDE
2. ติดตั้งบอร์ด `Raspberry Pi Pico/RP2040` (ของ Earle F. Philhower) ผ่าน Board Manager
3. ติดตั้งไลบรารีเพิ่มเติมคือ:
   - **ArduinoJson** (ตรวจจับและจัดการข้อมูลฟอร์แมต JSON)
   - **Servo** (ควบคุมมอเตอร์สำหรับการเปิด/ปิดฝา)
4. ปรับแก้ไขค่าคงที่ส่วนเชื่อมต่อ **WiFi** ให้ตรงกับชื่อเน็ตเวิร์กและรหัสผ่านที่จะใช้:
   ```cpp
   const char* WIFI_SSID = "เปลี่ยนเป็นชื่อWiFiของคุณ";
   const char* WIFI_PASS = "เปลี่ยนเป็นรหัสผ่านWiFi";
   ```
5. แก้ไขโค้ดในตัวแปรส่วนของการสื่อสารกับ Backend API:
   ```cpp
   const char* SERVER_URL    = "https://<ลิงก์_Vercel_ของคุณ>.vercel.app";
   const char* DEVICE_SECRET = "<ต้องตรงกับตัวแปร DEVICE_SECRET ใน .env>";
   ```
6. กดเลือก Upload เพื่อคอมไพล์และอัปโหลดข้อมูลลงบอร์ด

---

## 📂 โครงสร้างอธิบายโฟลเดอร์หลักของโปรเจกต์ (Directory Structure)
- `/api/` : เซิร์ฟเวอร์และ API (Serverless APIs บน Vercel) เพื่อจัดการฐานข้อมูลจาก Frontend หรือ Hardware 
  - `auth.js` : สำหรับการยืนยันตัวตน  
  - `device.js` : ศูนย์กลางการเข้าถึงสถานะถังและบันทึกประวัติการกระทำต่างๆ
- `/public/` : โฟลเดอร์หน้าบ้าน (Frontend Web) ผสานโค้ดฝั่งชุดเครื่องมือฮาร์ดแวร์  
  - `index.html` : หน้าเว็บการแสดงผลระบบแผงควบคุม Dashboard Management
  - `smart_trash_bin.ino` : ตัวประมวลผลการทำงานหลักที่อัปโหลดเข้าบอร์ด Pico W
- `supabase_schema.sql` : ไฟล์ชุดคำสั่งเพื่อเริ่มต้นสร้างตารางฐานข้อมูลและข้อมูลดิบ
